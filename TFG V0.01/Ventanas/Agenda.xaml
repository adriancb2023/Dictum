<Window x:Class="TFG_V0._01.Ventanas.Agenda" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:TFG_V0._01.Ventanas" xmlns:controls="clr-namespace:TFG_V0._01.Controladores" mc:Ignorable="d" Title="Agenda" Height="850" Width="1200" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Estilo para el botón de cambio de tema -->
        <Style x:Key="ThemeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <Image x:Name="ThemeIcon" Source="/TFG V0.01;component/Recursos/Iconos/sol.png" Width="20" Height="20"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Botones redondeados -->
        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="20" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="aqua" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" Value="#DDDDDD" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Estilo de tarjeta -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#454545" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#333" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Estilo para TextBox redondeado -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Estilo para ComboBox redondeado -->
        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" BorderThickness="0" Padding="{TemplateBinding Padding}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="20"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" VerticalAlignment="Center"/>
                                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="White" StrokeThickness="2" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup x:Name="Popup" AllowsTransparency="True" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom">
                                    <Border Background="#FF2D2D2D" CornerRadius="10" BorderThickness="1" BorderBrush="#40FFFFFF">
                                        <ScrollViewer MaxHeight="200">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Estilo para DatePicker redondeado -->
        <Style x:Key="RoundedDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <DatePickerTextBox x:Name="PART_TextBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Estilo para ListBox redondeado -->
        <Style x:Key="RoundedListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ScrollViewer>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="AUTO" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Fondo -->
        <Grid.Background>
            <ImageBrush x:Name="backgroundFondo" ImageSource="/TFG V0.01;component/Recursos/Background/oscuro/main.png"/>
        </Grid.Background>
        <!-- Navbar -->
        <controls:NavbarControl x:Name="navbar" Grid.Column="0" />
        <!-- Claro/Oscuro -->
        <Button x:Name="ThemeButton" Width="35" Height="35" Margin="0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="ThemeButton_Click" Style="{StaticResource ThemeButtonStyle}" Grid.Column="2"/>
        <!-- Contenido principal -->
        <Grid Grid.Column="2" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Header -->
            <TextBlock Text="Agenda" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
            <!-- Contenido de la agenda -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <!-- Calendario y Eventos del día seleccionado -->
                <Border Style="{StaticResource CardStyle}" Grid.Column="0" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- Calendario -->
                        <Calendar x:Name="MainCalendar" Grid.Row="0" Margin="0,0,0,15" FirstDayOfWeek="Monday" MouseDoubleClick="MainCalendar_MouseDoubleClick" SelectedDatesChanged="MainCalendar_SelectedDatesChanged"/>
                        <!-- Lista de eventos del día seleccionado -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Eventos del día seleccionado" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                <ListView x:Name="EventosDelDiaList" ItemsSource="{Binding EventosDelDia}" Background="Transparent" BorderThickness="0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#40FFFFFF" CornerRadius="10" Margin="0,0,0,10" Padding="10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Titulo}" FontWeight="Bold" Foreground="White" Grid.Row="0" Grid.Column="0"/>
                                                    <TextBlock Text="{Binding FechaInicio, StringFormat='{}{0:hh\\:mm}'}" Foreground="White" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>
                                                    <TextBlock Text="{Binding Descripcion}" Foreground="White" Grid.Row="1" Grid.Column="0" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                                    <TextBlock Text="{Binding IdCaso, StringFormat='Caso #{0}'}" Foreground="White" Grid.Row="2" Grid.Column="0" Margin="0,5,0,0"/>
                                                    <Border Background="{Binding EstadoColor}" CornerRadius="5" Width="10" Height="10" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
                <!-- Eventos de hoy -->
                <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Eventos de Hoy" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <ListView x:Name="EventosDeHoyList" ItemsSource="{Binding EventosDeHoy}" Background="Transparent" BorderThickness="0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#40FFFFFF" CornerRadius="10" Margin="0,0,0,10" Padding="10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Titulo}" FontWeight="Bold" Foreground="White" Grid.Row="0" Grid.Column="0"/>
                                                    <TextBlock Text="{Binding FechaInicio, StringFormat='{}{0:hh\\:mm}'}" Foreground="White" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>
                                                    <TextBlock Text="{Binding Descripcion}" Foreground="White" Grid.Row="1" Grid.Column="0" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                                    <TextBlock Text="{Binding IdCaso, StringFormat='Caso #{0}'}" Foreground="White" Grid.Row="2" Grid.Column="0" Margin="0,5,0,0"/>
                                                    <Border Background="{Binding EstadoColor}" CornerRadius="5" Width="10" Height="10" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
                <!-- Lista de contactos -->
                <Border Style="{StaticResource CardStyle}" Grid.Column="2" Margin="10,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Contactos" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- Aquí se cargarán los contactos -->
                            </StackPanel>
                        </ScrollViewer>
                        <Button Grid.Row="2" Content="Nuevo Contacto" Style="{StaticResource RoundedButtonStyle}" Background="#40FFFFFF" Foreground="White" Margin="0,15,0,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        <!-- Panel deslizable para nuevo evento -->
        <Grid x:Name="NewEventPanel" Grid.Column="2" HorizontalAlignment="Right" Width="400" Background="#FF2D2D2D" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Header del panel -->
            <Grid Background="#FF1E1E1E" Height="60">
                <TextBlock Text="Nuevo Evento" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="20,0"/>
                <Button x:Name="ClosePanelButton" Content="×" FontSize="24" Foreground="White" HorizontalAlignment="Right" Width="60" Height="60" Background="Transparent" BorderThickness="0" Click="ClosePanelButton_Click"/>
            </Grid>
            <!-- Contenido del formulario -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <!-- Título del evento -->
                    <TextBlock Text="Título" Foreground="White" Margin="0,0,0,5"/>
                    <TextBox x:Name="EventTitleTextBox" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,15"/>
                    <!-- Fecha y hora -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Fecha" Foreground="White" Margin="0,0,0,5"/>
                            <DatePicker x:Name="EventDatePicker" Style="{StaticResource RoundedDatePickerStyle}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Hora" Foreground="White" Margin="0,0,0,5"/>
                            <ComboBox x:Name="EventTimeComboBox" Style="{StaticResource RoundedComboBoxStyle}"/>
                        </StackPanel>
                    </Grid>
                    <!-- Descripción -->
                    <TextBlock Text="Descripción" Foreground="White" Margin="0,0,0,5"/>
                    <TextBox x:Name="EventDescriptionTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>
                    <!-- Ubicación -->
                    <TextBlock Text="Ubicación" Foreground="White" Margin="0,0,0,5"/>
                    <TextBox x:Name="EventLocationTextBox" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,15"/>
                    <!-- Participantes -->
                    <TextBlock Text="Participantes" Foreground="White" Margin="0,0,0,5"/>
                    <ListBox x:Name="ParticipantsListBox" Style="{StaticResource RoundedListBoxStyle}" Height="100" Margin="0,0,0,15"/>
                    <Button Content="Añadir Participante" Style="{StaticResource RoundedButtonStyle}" Background="#40FFFFFF" Foreground="White" Margin="0,0,0,15"/>
                </StackPanel>
            </ScrollViewer>
            <!-- Botones de acción -->
            <Grid Grid.Row="2" Background="#FF1E1E1E" Height="70">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0">
                    <Button Content="Cancelar" Style="{StaticResource RoundedButtonStyle}" Background="#40FFFFFF" Foreground="White" Margin="0,0,10,0" Click="CancelEventButton_Click"/>
                    <Button Content="Guardar" Style="{StaticResource RoundedButtonStyle}" Background="#40FFFFFF" Foreground="White" Click="SaveEventButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>