<Window x:Class="TFG_V0._01.Ventanas.Clientes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TFG_V0._01.Controladores"
        xmlns:local="clr-namespace:TFG_V0._01.Ventanas"
        xmlns:conv="clr-namespace:TFG_V0._01.Converters"
        mc:Ignorable="d"
        Title="Clientes" Height="850" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        
        <!-- Estilo para el botón de cambio de tema -->
        <Style x:Key="ThemeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <Image x:Name="ThemeIcon" Source="/TFG V0.01;component/Recursos/Iconos/sol.png" Width="20" Height="20"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Botones redondeados -->
        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                    CornerRadius="20" BorderThickness="{TemplateBinding BorderThickness}" 
                    BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="aqua" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" Value="#DDDDDD" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Easing para suavizar la animación -->
        <QuadraticEase x:Key="SmoothEase" EasingMode="EaseInOut" />
        
        <!-- Estilo ComboBox moderno -->
        <Style x:Key="ModernComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                                <ToggleButton
                                    Name="MainToggle"
                                    Grid.Column="0"
                                    Focusable="False"
                                    IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    Background="Transparent"
                                    BorderThickness="0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="30"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter
                                            Grid.Column="0"
                                            Content="{TemplateBinding SelectedItem}"
                                            ContentTemplate="{TemplateBinding ItemTemplate}"
                                            Margin="10,0,0,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Stretch"/>
                                        <Path
                                            Grid.Column="1"
                                            Data="M0,0 L4,4 L8,0"
                                            Stroke="White"
                                            StrokeThickness="2"
                                            Width="20"
                                            Height="20"
                                            Margin="0,0,10,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right"/>
                                    </Grid>
                                </ToggleButton>
                            </Border>
                            <Popup 
                                IsOpen="{TemplateBinding IsDropDownOpen}" 
                                Placement="Bottom"
                                AllowsTransparency="True" 
                                Focusable="False"
                                PopupAnimation="Slide"
                                Panel.ZIndex="9999">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      MinWidth="{Binding Path=ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border Background="#90454545" 
                                            BorderBrush="#40FFFFFF"
                                            BorderThickness="1" 
                                            CornerRadius="10" 
                                            Margin="0,5,0,0"
                                            Effect="{StaticResource DropShadowEffect}">
                                        <ScrollViewer SnapsToDevicePixels="True"
                                                    Margin="5">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para el ToggleButton del ComboBox -->
        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border Background="Transparent" Width="30">
                            <Path Data="M0,0 L4,4 L8,0" Stroke="White" StrokeThickness="2"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo de tarjeta -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#454545" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#40FFFFFF" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <DropShadowEffect x:Key="DropShadowEffect" Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.3"/>

        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <conv:EstadoToColorConverter x:Key="EstadoToColorConverter"/>
        <conv:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

        <Style x:Key="ModernDetailsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#80FFFFFF"/>
                                <Setter Property="Foreground" Value="#0078D7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para estirar los ítems del ListView -->
        <Style x:Key="StretchingListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>

        <Style x:Key="ComboBoxWithClearStyle" TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}">
            <Setter Property="Width" Value="400"/>
            <Setter Property="IsEditable" Value="True"/>
            <Setter Property="StaysOpenOnEdit" Value="True"/>
            <Setter Property="IsTextSearchEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="35"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="PART_EditableTextBox"
                                            Grid.Column="0"
                                            Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                            Background="Transparent"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="10,0,0,0"
                                            VerticalAlignment="Center"/>
                                    <!-- Botón X -->
                                    <Button x:Name="ClearButton"
                                            Grid.Column="1"
                                            Content="✖"
                                            Width="25" Height="25"
                                            Margin="5,0,0,0"
                                            Foreground="Red"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Visibility="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}"
                                            Click="ClearComboBox_Click"/>
                                    <!-- Flecha desplegable -->
                                    <ToggleButton
                                        Grid.Column="2"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Focusable="False">
                                        <ToggleButton.Style>
                                            <Style TargetType="ToggleButton" BasedOn="{StaticResource ComboBoxToggleButtonStyle}"/>
                                        </ToggleButton.Style>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup 
                                IsOpen="{TemplateBinding IsDropDownOpen}" 
                                Placement="Bottom"
                                AllowsTransparency="True" 
                                Focusable="False"
                                PopupAnimation="Slide"
                                Panel.ZIndex="9999">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      MinWidth="{Binding Path=ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border Background="#90454545" 
                                            BorderBrush="#40FFFFFF"
                                            BorderThickness="1" 
                                            CornerRadius="10" 
                                            Margin="0,5,0,0"
                                            Effect="{StaticResource DropShadowEffect}">
                                        <ScrollViewer SnapsToDevicePixels="True"
                                                    Margin="5">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="AUTO" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Fondo -->
        <Grid.Background>
            <ImageBrush x:Name="backgroundFondo" ImageSource="/TFG V0.01;component/Recursos/Background/oscuro/main.png"/>
        </Grid.Background>
        
        <!-- Navbar -->
        <controls:NavbarControl x:Name="navbar" Grid.Column="0" />
        
        <!-- Claro/Oscuro  -->
        <Button x:Name="ThemeButton" Width="35" Height="35" Margin="0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="ThemeButton_Click" Style="{StaticResource ThemeButtonStyle}" Grid.Column="2"/>

        <!-- Contenido Principal -->
        <Grid Grid.Column="2" Margin="20">

            <!-- Selector de Cliente -->
            <Grid x:Name="ClientSelectorGrid" Visibility="Visible">

                <Border Style="{StaticResource CardStyle}" MaxWidth="600" MaxHeight="600" VerticalAlignment="Center" Margin="0">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Contenedor superior: título + combo -->
                        <StackPanel Grid.Row="0" Margin="20">

                            <TextBlock Text="Seleccionar Cliente"  FontSize="24" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                            <!-- ComboBox con altura colapsable -->
                            <Grid x:Name="ComboBoxContainer" Height="40">

                                <!-- contraído por defecto -->
                                <ComboBox x:Name="ClientesComboBox"  Style="{StaticResource ComboBoxWithClearStyle}" IsEditable="True"  StaysOpenOnEdit="True"  IsTextSearchEnabled="False" MaxDropDownHeight="300" Loaded="ComboClientes_Loaded" KeyUp="ComboClientes_KeyUp" SelectionChanged="ComboClientes_SelectionChanged" 
                                          DropDownOpened="ClientesComboBox_DropDownOpened" DropDownClosed="ClientesComboBox_DropDownClosed" ItemsSource="{Binding ListaClientes}" SelectedItem="{Binding SelectedCliente, Mode=TwoWay}"/>

                            </Grid>

                        </StackPanel>

                        <!-- Contenedor de botones -->
                        <StackPanel Grid.Row="1" Margin="20,10,20,20">

                            <Button Content="Continuar" Style="{StaticResource RoundedButtonStyle}"  Background="White" Foreground="Black" HorizontalAlignment="Stretch" MaxWidth="400" Margin="0,0,0,10" Click="clientecheck"/>

                            <Button Content="Nuevo Cliente" Style="{StaticResource RoundedButtonStyle}"  Background="#40FFFFFF" Foreground="White" HorizontalAlignment="Stretch"  MaxWidth="400"/>

                        </StackPanel>

                    </Grid>

                </Border>

            </Grid>

            <!-- Datos -->
            <Grid x:Name="ClientDetailsGrid" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Cabecera con botón volver -->
                <Grid Grid.Row="0" Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" 
                            Style="{StaticResource RoundedButtonStyle}" 
                            Background="#40FFFFFF" 
                            Foreground="White" 
                            Margin="0,0,20,0"
                            Height="60"
                            Width="100"
                            Click="atras">
                        <StackPanel Orientation="Horizontal">
                            <Image x:Name="volver" Source="/TFG V0.01;component/Recursos/Iconos/volver.png" 
                                   Width="24" 
                                   Height="24" 
                                   VerticalAlignment="Center"/>
                            <TextBlock Text="Volver" 
                                       Margin="10,0,0,0" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <TextBlock Grid.Column="1" 
                               x:Name="ClienteNombreHeader" 
                               Text="{Binding SelectedCliente.nombre_cliente}"
                               FontSize="24" 
                               Foreground="White" 
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Grid>
                
                <!-- Contenido de los detalles -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Columna Izquierda -->
                        <!-- Información Personal -->
                        <Border Style="{StaticResource CardStyle}" Grid.Column="0" Grid.Row="0" Margin="0,0,0,10">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Direction="270" Color="#66000000" BlurRadius="10" Opacity="0.3"/>
                            </Border.Effect>
                            <Grid>
                                <!-- Vista normal (no edición) -->
                                <StackPanel x:Name="PersonalInfoPanel"
                                                Visibility="{Binding IsEditPersonalInfoVisible, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                    <TextBlock Text="{Binding SelectedCliente.fecha_contrato, StringFormat='Fecha de contrato: {0:dd/MM/yyyy}'}"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   FontSize="16"
                                                   Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding SelectedCliente.nombre_cliente}" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                                    <TextBlock Text="DNI:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.id}" Foreground="White" Margin="0,0,0,8"/>
                                    <TextBlock Text="Teléfono 1:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.telf1}" Foreground="White" Margin="0,0,0,8"/>
                                    <TextBlock Text="Teléfono 2:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.telf2}" Foreground="White" Margin="0,0,0,8"/>
                                    <TextBlock Text="Email 1:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.email1}" Foreground="White" Margin="0,0,0,8"/>
                                    <TextBlock Text="Email 2:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.email2}" Foreground="White" Margin="0,0,0,8"/>
                                    <TextBlock Text="Dirección:" Foreground="#CCFFFFFF" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding SelectedCliente.direccion}" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,8"/>
                                    <Button Content="Editar Información"
                                                Style="{StaticResource RoundedButtonStyle}"
                                                Background="#40FFFFFF"
                                                Foreground="White"
                                                HorizontalAlignment="Right"
                                                Margin="0,10,0,0"
                                                Click="EditarInformacion_Click" Height="45" Width="150"/>
                                </StackPanel>

                                <!-- Panel de edición -->
                                <Border x:Name="EditPersonalInfoPanel"
                                            Background="#CC222222"
                                            CornerRadius="15"
                                            Padding="20"
                                            Visibility="{Binding IsEditPersonalInfoVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                            VerticalAlignment="Stretch"
                                            HorizontalAlignment="Stretch"
                                            Opacity="0.97">
                                    <StackPanel>
                                        <TextBlock Text="{Binding EditFechaContrato, StringFormat='Fecha de contrato: {0:dd/MM/yyyy}'}"
                                                       FontWeight="Bold"
                                                       Foreground="White"
                                                       FontSize="16"
                                                       Margin="0,0,0,10"/>
                                        <TextBlock Text="Nombre:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditNombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Primer Apellido:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditApellido1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Segundo Apellido:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditApellido2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="DNI:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditDNI, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Teléfono 1:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditTelefono1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Teléfono 2:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditTelefono2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Email 1:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditEmail1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Email 2:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditEmail2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                        <TextBlock Text="Dirección:" Foreground="#CCFFFFFF" Margin="0,0,0,5"/>
                                        <TextBox Text="{Binding EditDireccion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="Guardar" Margin="0,10,10,0" Click="GuardarInformacion_Click"/>
                                            <Button Content="Cancelar" Margin="0,10,0,0" Click="CancelarInformacion_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- Casos Activos -->
                        <Border Style="{StaticResource CardStyle}" Grid.Column="0" Grid.Row="2" Margin="0,0,0,10">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Direction="270" Color="#66000000" BlurRadius="10" Opacity="0.3"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Casos Activos" FontSize="20" Foreground="White" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="Nuevo Caso" Style="{StaticResource RoundedButtonStyle}" Background="#40FFFFFF" Foreground="White" Height="45" Width="120" Padding="15,0"/>
                                </Grid>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300" Margin="0,10,0,0">
                                    <ListView x:Name="CasosActivosList"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Margin="0,15,0,0"
                                                  ItemsSource="{Binding CasosActivos}"
                                                  ItemContainerStyle="{StaticResource StretchingListViewItemStyle}">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#50FFFFFF"
                                                            CornerRadius="16"
                                                            Padding="18"
                                                            Margin="0,8"
                                                            Effect="{StaticResource DropShadowEffect}"
                                                            HorizontalAlignment="Stretch">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <!-- Círculo de estado -->
                                                        <Ellipse Width="18" Height="18" Margin="0,0,12,0" Grid.Column="0">
                                                            <Ellipse.Fill>
                                                                <Binding Path="Estado.nombre" Converter="{StaticResource EstadoToColorConverter}"/>
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                        <!-- Info del caso -->
                                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding titulo}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                                            <TextBlock Text="{Binding Estado.nombre}" Foreground="#FFD6D6D6" FontSize="13" Margin="0,2,0,0"/>
                                                        </StackPanel>
                                                        <!-- Botón detalles -->
                                                        <Button Grid.Column="2"
                                                                    Content="Ver Detalles"
                                                                    Style="{StaticResource ModernDetailsButtonStyle}"
                                                                    Command="{Binding DataContext.VerDetallesCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    VerticalAlignment="Center"
                                                                    Margin="12,0,0,0" Height="45" Width="120"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>

                        <!-- Historial de Casos -->
                        <Border Style="{StaticResource CardStyle}" Grid.Column="0" Grid.Row="4" Margin="0,0,0,10">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Direction="270" Color="#66000000" BlurRadius="10" Opacity="0.3"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="Historial de Casos" FontSize="20" Foreground="White" Margin="0,0,0,15"/>
                                <ListView x:Name="HistorialCasosList"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              MaxHeight="300"
                                              ItemsSource="{Binding HistorialCasos}"
                                              ItemContainerStyle="{StaticResource StretchingListViewItemStyle}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#50FFFFFF"
                                                        CornerRadius="16"
                                                        Padding="18"
                                                        Margin="0,8"
                                                        Effect="{StaticResource DropShadowEffect}"
                                                        HorizontalAlignment="Stretch">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <!-- Círculo de estado -->
                                                    <Ellipse Width="18" Height="18" Margin="0,0,12,0" Grid.Column="0">
                                                        <Ellipse.Fill>
                                                            <Binding Path="Estado.nombre" Converter="{StaticResource EstadoToColorConverter}"/>
                                                        </Ellipse.Fill>
                                                    </Ellipse>
                                                    <!-- Info del caso -->
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding titulo}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                                        <TextBlock Text="{Binding Estado.nombre}" Foreground="#FFD6D6D6" FontSize="13" Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Border>

                        <!--columna izquierda-->
                        <!-- Documentos del Cliente -->
                        <Border Style="{StaticResource CardStyle}" Grid.Column="2" Grid.Row="0" Grid.RowSpan="5" Margin="0,0,0,10">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Direction="270" Color="#66000000" BlurRadius="10" Opacity="0.3"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="Documentos del Cliente" FontSize="20" Foreground="White" Margin="0,0,0,15"/>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="1150">
                                    <ListView x:Name="DocumentosClienteList"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  ItemsSource="{Binding DocumentosCliente}"
                                                  ItemContainerStyle="{StaticResource StretchingListViewItemStyle}">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#50FFFFFF"
                                                            CornerRadius="16"
                                                            Padding="18"
                                                            Margin="0,8"
                                                            Effect="{StaticResource DropShadowEffect}"
                                                            HorizontalAlignment="Stretch">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding nombre}" Foreground="White" FontWeight="Bold" FontSize="15"/>
                                                            <TextBlock Text="{Binding fecha_subid, StringFormat='Subido: {0:dd/MM/yyyy}'}" Foreground="#FFD6D6D6" FontSize="12"/>
                                                            <TextBlock Text="{Binding tipo_documento}" Foreground="#FFD6D6D6" FontSize="12"/>
                                                        </StackPanel>
                                                        <Button Grid.Column="1" Content="Ver" Style="{StaticResource ModernDetailsButtonStyle}" Height="45" Width="100" FontSize="12" Margin="8,0,0,0" Click="VerDocumento_Click"/>
                                                        <Button Grid.Column="2" Content="Descargar" Style="{StaticResource ModernDetailsButtonStyle}" Height="45" Width="100" FontSize="12" Margin="8,0,0,0" Click="DescargarDocumento_Click"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Grid>

        </Grid>
    </Grid>
</Window>
